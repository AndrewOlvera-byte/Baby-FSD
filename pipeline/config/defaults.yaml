defaults:
  - exp: bc_train
  - trainer: bc
  - modes: train
  - speed: default
  - seed: default
  - logger: offline
  - evaluator: carla_eval
  - _self_

hydra:
  run:
    dir: "outputs/${exp.name}/${modes.mode}/${now:%Y%m%d_%H%M%S}"
  output_subdir: null

exp:
  name: "bc_train"
  desc: "Behavior Cloning training"
  tags: ["bc", "behavior_cloning"]
  notes: ""
