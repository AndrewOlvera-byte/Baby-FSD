# === Core ===
# Use torch/vision/audio from the base image to keep CUDA compatibility
# Align with the pytorch/pytorch:2.7.0-cuda12.8-cudnn9 base image (ships torchvision 0.22.x)
torchvision>=0.22.0,<0.23.0
torchrl>=0.8.0  # TorchRL for offline RL training (compatible with PyTorch 2.7.0+)
tensordict>=0.7.0  # Required by TorchRL

# === Testing ===
pytest>=7.0.0
h5py>=3.9.0
hdf5plugin>=4.0.0

# === Hydra & HPO ===
hydra-core==1.3.*
omegaconf==2.3.*
hydra-optuna-sweeper==1.2.0
optuna>=2.10.1,<3.0.0        # pinned for hydra-optuna-sweeper 1.2.0 compatibility
SQLAlchemy<2.0


# === Logging ===
tqdm==4.*
rich>=12.0,<14.0

# === Data / Utils ===
# Use versions compatible with Python 3.11 (Docker base image)
numpy>=1.26.0,<2.0
pandas>=2.1.0,<2.2
scipy>=1.11.0,<1.13.0
pyyaml>=6.0
matplotlib>=3.5.0,<3.6.0
pyarrow>=12.0
duckdb>=0.8
webdataset>=0.2.100  # WebDataset for efficient data loading (0.2.100+ has WebLoader improvements)

# === YOLO ===
# ultralytics>=8.3.0           # YOLOv11 segmentation support - requires Python 3.8+

# === Docker safety / reproducibility ===
# versions below ensure deterministic builds
protobuf<4.0
